public class Settings {
    private static String displayChar;
    private boolean displayNum = true;

    public static void changeDisplayChar(String dChar) {
        if (dChar.equals("x")) {
            displayChar = "XX";
        } else {
            displayChar = MemorySequence.;
        }
    }
}
